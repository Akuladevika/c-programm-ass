#include<stdio.h>
int main(void){
	int T;
	scanf("%d",&T);
	while(T--)
	{
		long int i, n,sum=0;
		scanf("%d",&n);
		long int a[n];
		for(i=0;i<n;i++)
		scanf("%d",&a[i]);
		for(i=0;i<n;i++)
		sum=sum+a[i];
		for(i=0;i<n;i++)
		printf("%d\n",sum-a[i]);
	
		
	}
}
